import React, { useState } from 'react';

const Countries = () => {
    const [date, setDate] = useState([])


    return (
        <div>
            Countries
        </div>
    );
};

export default Countries;